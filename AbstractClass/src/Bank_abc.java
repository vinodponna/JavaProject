
public class Bank_abc extends Bank{ // if we don't add abstract in front of class then by default its concrete.
	 int getInterRate()
	 {
		 return 5;
	 }		//giving definition of abstract method from super class to subclass.
	
	
}
